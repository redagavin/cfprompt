# Your cfprompt study

This directory was scaffolded by `cfprompt init`.

## Files

- `config.yaml` — study configuration (edit to match your data)
- `perturbations.py` — define your target perturbation here
- `data/` — drop your CSV/Parquet here

## Run

```bash
cfprompt validate config.yaml         # check schema + imports
cfprompt run config.yaml              # execute the study
```
